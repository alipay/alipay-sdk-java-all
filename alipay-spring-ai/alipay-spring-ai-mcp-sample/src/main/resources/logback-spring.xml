<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- MCP 传输专用日志文件 -->
    <appender name="MCP_TRANSPORT_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/mcp-transport.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/mcp-transport.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <maxFileSize>100MB</maxFileSize>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- HTTP 请求/响应日志 -->
    <appender name="MCP_HTTP_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/mcp-http.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>logs/mcp-http.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxHistory>7</maxHistory>
            <maxFileSize>100MB</maxFileSize>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Logger 配置 -->
    <logger name="com.alipay.mcp.springai.transport" level="DEBUG" additivity="false">
        <appender-ref ref="MCP_TRANSPORT_FILE"/>
        <appender-ref ref="CONSOLE"/>
    </logger>

    <logger name="com.alipay.mcp.springai.transport.http" level="TRACE" additivity="false">
        <appender-ref ref="MCP_HTTP_FILE"/>
    </logger>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>